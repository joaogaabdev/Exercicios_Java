package exercicios;

public class ExercicioString01 {
    public static void main(String args []) {
        //Formatar:
            // toLowerCase() -> deixar a letra minúscula
            // toUpperCase() -> deixar a letra maiúscula
            // trim() -> remover espaços
        //Recortar:
            // substring(inicio) ->
            // substring(inicio, fim) ->
        //Substituir:
            // Replace(char, char), Replace(string, string)
        //Buscar:
            // IndexOf, LastIndexOf
            //str.Split("")
        String original = "abcde FGHIJ ABC abc DEFG    ";

    }
}
